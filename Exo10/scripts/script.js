var mealsWrapper = document.getElementById('meals');

function getmeals() {
}

function displaymeals(mealsArray) {
    mealsArray.forEach(meal => {

    });
    mealDisplay.append(mealPoster, mealInfo, mealDescription);
    return mealDisplay;
}